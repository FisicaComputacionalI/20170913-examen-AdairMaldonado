import numpy as np
import pylab as pl

x = np.linspace(-3*np.pi, 3*np.pi, 100)
y = 3*np.sin(2014+x)

pl.title('Funcion trigonometrica')
pl.scatter(x, y, s=60, c='r')
pl.savefig('Ejercicio 2.png')
pl.show()
